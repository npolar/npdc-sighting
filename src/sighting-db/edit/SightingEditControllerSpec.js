'use strict';
require('should');

describe('SightingEditController', function () {
  var $scope, $controller, $routeParams, Sighting;

  // Set up mocks
  before(function () {
    $scope = {};
    $routeParams = {};
    Sighting = {};
    $controller = function () {};
  });

  describe('#initFormula', function () {
    // no test...
  });
});
